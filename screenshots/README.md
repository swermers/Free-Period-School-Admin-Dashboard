# screenshots/

Place product screenshots here and reference them from the top-level
`README.md`. Suggested captures:

- `dashboard-preview.png` — overall dashboard with a PowerSchool-style layout.
- `upload.png` — upload + schema detection.
- `correlation.png` — correlation heatmap card.
- `pdf-report.png` — first page of an exported PDF.

Take screenshots from a real browser session; the dashboard uses Chart.js and
html2canvas which require a real rendering context. Save as PNG at 2x DPI for
retina displays.
